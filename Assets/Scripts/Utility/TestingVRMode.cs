using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingVRMode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		GlobalValues.controllerMode = ControllerMode.VR;
    }

}
